import styled from 'styled-components';
import { animated } from 'react-spring';

export const HomeComponent = styled(animated.div)`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
`